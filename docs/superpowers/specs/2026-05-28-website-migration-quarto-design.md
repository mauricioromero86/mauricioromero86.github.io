# Website Migration: WordPress (Bluehost) → Quarto on GitHub Pages

**Date:** 2026-05-28
**Owner:** Mauricio Romero
**Status:** Design — awaiting approval

## Goal

Replace the WordPress/Mesmerize site at `mauricio-romero.com` (Bluehost) with a
static Quarto site hosted on GitHub Pages. Outcome: all content editable as plain
Markdown in a git repo, no database, no server, free hosting, and Bluehost
eventually dropped.

## Why this approach

- **Quarto** matches the owner's existing R/Stata academic toolchain, runs cleanly
  on Windows via a single installer, and uses Markdown content — exactly the
  hand-edited, per-entry publication workflow requested.
- **GitHub Pages** is free, git-native, and integrates with a GitHub Action so a
  push auto-builds and deploys (Quarto local needed only for `quarto preview`).
- Alternatives rejected: **al-folio (Jekyll)** — gold-standard look but its
  signature feature is `.bib`-driven publications (not wanted) and it needs a
  Ruby/Jekyll toolchain that is painful on Windows. **Hugo** — least friendly
  templating, fragmented academic theme ecosystem post-Wowchemy.

## Decisions (locked)

| Topic | Decision |
|-------|----------|
| Framework | Quarto |
| Host | GitHub Pages |
| Publications | Hand-written per-entry Markdown blocks (no `.bib` pipeline) |
| Design | Clean academic Bootswatch theme (default `litera` or `cosmo`) + light `styles.scss` |
| PDFs (papers + teaching) | In-repo, Ghostscript-compressed to fit under ~1 GB; prune oldest course-years if still over |
| Data & Code | Datasets (`data/`) + replication packages (`replicationData/`) **linked externally** (Dataverse/OSF/Zenodo/journal), not hosted. `polucion/` maps migrated in-repo. `Map_IPS.html` **dropped** (deferred rebuild — broken Google Maps deps) |
| URL preservation | Keep paper PDFs at exact paths `/pdfs/papers/<file>.pdf` (cited externally). GitHub Pages has no server-side 301s, so paths must match |
| Analytics | Legacy Google UA property is dead → add **GA4** (or omit) |
| Contact | Email + Scholar/Twitter/Bluesky links; no form |
| GitHub account | `mauricioromero86` (the bare `mauricioromero` username is taken by another user, so `mauricioromero.github.io` is unavailable) |
| Test URL | `mauricioromero86.github.io` (user-site repo, serves at root `/`) |
| Domain cutover | Launch on `mauricioromero86.github.io` first → verify → point DNS to Pages. Add redirect on old Bluehost site during transition until host/domain lapses |
| SEO/AI | Full SEO per Google starter guide + AI optimization guide (see SEO section). **Revised 2026-05-31:** a root `llms.txt` + per-paper machine-readable `.pdf.md` twins **are** maintained (owner opted into Quentin André's "legible to machines" approach); no other AI-specific markup |

## Site structure

Mirrors the current four sections: Bio/CV (home), Research, Data & Code, Teaching.

```
mauricioromero86.github.io/     (new git repo, repo root; user-site repo)
├── _quarto.yml                 # site config: theme, navbar, site-url, OG/cards
├── index.qmd                   # Bio/CV homepage (+ Person/ProfilePage JSON-LD)
├── research.qmd                # Publications (hand-written entries)
├── data-code.qmd               # Data & Code (external dataset/replication links + polucion maps)
├── teaching.qmd                # Teaching (links to course PDFs)
├── styles.scss                 # accent color, fonts
├── robots.txt                  # allow all crawlers + sitemap pointer
├── CNAME                       # custom domain (added at cutover)
├── pdfs/                       # papers, CV, teaching notes (compressed)
│   └── papers/                 # paths preserved exactly: /pdfs/papers/<file>.pdf
├── polucion/                   # 16 self-contained Bogotá pollution maps (migrated as-is)
├── img/                        # profile photo + images
└── .github/workflows/publish.yml
# sitemap.xml is auto-generated by Quarto from site-url
# data/ and replicationData/ are NOT in the repo — linked externally
# Map_IPS.html is NOT migrated (broken deps; deferred rebuild)
```

The existing `public_html/` WordPress download remains on disk as source material
but is excluded from the site repo (`.gitignore`), not deleted.

## Content model

Each page is a `.qmd` Markdown file. A publication is a repeatable block the owner
copies and edits — no central index, no build step beyond Quarto's render:

```markdown
### Paper Title
*with Coauthor A, Coauthor B* · *Journal*, Year (or "Forthcoming")

[PDF](pdfs/papers/file.pdf) · [Journal](https://doi.org/...) · [BibTeX](#bibtex-key)

> Abstract text (optional, collapsible via callout).
```

Adding a paper = paste a block + drop the PDF in `pdfs/papers/`.

## PDF / asset handling

- Total source content is **~3.3 GB**: `pdfs/` 2.3 GB, `data/` 586 MB,
  `replicationData/` 271 MB, `Map_IPS.html` 82 MB, `polucion/` 44 MB.
- **Off-repo (linked externally):** `data/` + `replicationData/` (~860 MB) → Dataverse/OSF/
  Zenodo or journal archives. `Map_IPS.html` dropped.
- **In-repo PDFs:** batch-compress with Ghostscript `-dPDFSETTINGS=/ebook` (expect 3–10×
  reduction on scan/figure-heavy files). Report before/after total size.
- Target: repo under ~1 GB. If still over after compression, prune oldest course-years
  (owner approves). Prune/compress targets: **EcoIV = 1.8 GB** (largest), esp. terms `20201`
  (363 MB), `20211` (449 MB), and `PastExams` (184 MB).
- Hard constraints respected: no single file > 100 MB (current max ~37 MB, fine).
- Paper PDFs keep their exact existing paths `/pdfs/papers/<file>.pdf` (see URL preservation).

## Content & SEO recovery

Page text, SEO titles/descriptions, and the headshot are **not in the FTP download** — they
live in the WordPress DB (`maurico8_WPFIY`) / media library. Current SEO is dynamic via the
**All-in-One SEO Pack** plugin (no `robots.txt`/`sitemap.xml` in files).

- Recover page text + AIOSEO `<title>`/`<meta description>` by **scraping the four live
  pages** (preferred), or export the DB via phpMyAdmin / `wp db export`.
- Pull the headshot from the live media library or request it from the owner.
- Cross-check every recovered publication against `CV-ENG.tex` (authoritative).

## SEO & AI optimization

Implements Google's SEO Starter Guide and AI Optimization Guide. Both reward the
same fundamentals (crawlable semantic HTML, clear structure, helpful content);
the AI guide advises against most AI-specific markup, content chunking, and
over-reliance on structured data — so none of those are used. **Exception (added
2026-05-31):** a root `llms.txt` and machine-readable `.pdf.md` twins of each paper
*are* published, following Quentin André's "legible to machines" approach (owner
request). Google's crawlers may ignore `llms.txt`, but it is cheap and used by other
agents; see CLAUDE.md "Machine-readable papers". No other AI-specific markup is added.

| Item | Implementation in Quarto |
|------|--------------------------|
| Unique title per page | `title:` front matter on each `.qmd` → `<title>` |
| Meta description per page | `description:` front matter → `<meta name="description">` |
| Canonical URLs + sitemap | set `site-url:` in `_quarto.yml` → Quarto auto-generates `sitemap.xml` and canonical tags |
| Social/AI link previews | `_quarto.yml`: `open-graph: true`, `twitter-card: true` |
| Structured data | JSON-LD `<script type="application/ld+json">` via `include-in-header`: `Person` + `ProfilePage` on homepage (name, jobTitle, affiliation=ITAM, `sameAs`=[Scholar, Twitter, Bluesky, ORCID, RePEc, OpenAlex], `@id`=`/#person`); `ScholarlyArticle` `@graph` for **all** publications on research.html via `_includes/research-jsonld.html`, generated by `tools/gen-research-jsonld.py` from `research.qmd` (implemented 2026-06-10 — standard schema.org per Google's guide, not AI-specific markup) |
| Crawler access | static `robots.txt` allowing all agents + `Sitemap:` line |
| Semantic headings | one `H1` per page, logical `H2/H3` (native Markdown) |
| Image alt text | descriptive `alt` on profile photo and any figures |
| Descriptive link text | meaningful anchors (e.g. "PDF", paper title) not "click here" |
| Mobile / performance | responsive Bootstrap theme + compressed PDFs → strong Core Web Vitals |
| Verification | Google Search Console: verify custom domain at cutover, submit `sitemap.xml` |

Explicitly NOT done (per Google's AI guide): AI-specific markup beyond `llms.txt`,
content chunking, keyword stuffing, inauthentic backlink building. *(`llms.txt` +
per-paper `.pdf.md` twins are maintained as a deliberate exception — see above.)*

## Deployment

- GitHub Action (`quarto-dev/quarto-actions`) builds on push to `main`, publishes
  rendered `_site/` to `gh-pages` branch; Pages serves `gh-pages`.
- Phase 1: site live at `https://mauricioromero86.github.io/` for verification.
- Phase 2 (cutover): add `CNAME` = `mauricio-romero.com`, update Bluehost DNS
  A/CNAME records to GitHub Pages IPs, enable HTTPS in repo Pages settings.
- Transition: place a redirect (`<meta http-equiv="refresh">` / `.htaccess`) on the
  old Bluehost `public_html` pointing to the new site, active until host lapses.

## Migration procedure (high level)

1. Install Quarto (`winget install --id Posit.Quarto -e`).
2. Init git repo at project root; create GitHub repo via `gh`.
3. Scaffold Quarto site (`_quarto.yml`, 4 `.qmd` pages, theme, GA4).
4. Scrape current page text + AIOSEO titles/descriptions from the live site → Markdown →
   owner reviews/corrects; obtain headshot.
5. Compress PDFs; copy into `pdfs/` keeping `/pdfs/papers/<file>.pdf` paths exact; migrate
   `polucion/` as-is; wire external links for `data/` + `replicationData/`.
6. Add GitHub Action; deploy to `mauricioromero86.github.io`; verify all pages, PDF links,
   and SEO/structured data.
7. Cutover: CNAME + DNS; verify HTTPS; add redirect on old site; Search Console + sitemap.
8. Decommission Bluehost once domain resolves to Pages and is verified.

## Tooling status

| Tool | Status |
|------|--------|
| git 2.53 | installed |
| winget | available |
| gh CLI 2.88 | installed |
| Quarto | NOT installed — single `winget` install |
| R / Python / LaTeX | not required (content-only site) |

## Out of scope

- `.bib`-driven automated publication lists.
- Blog/news feed (not present on current site).
- Contact form processing.
- Migrating WordPress core, plugins, or the MySQL database.
- `Map_IPS.html` (deferred rebuild — broken Google Maps deps, 82 MB).
- Hosting raw datasets / replication packages in-repo (linked externally instead).

## Verification criteria

- All four pages render and match the current site's content.
- Every PDF link resolves (script-checked against the file list).
- Site total under ~1 GB.
- Site live and HTTPS-valid on the custom domain.
- Old site redirects to the new one during transition.
- SEO/AI: every page has unique title + description; `sitemap.xml` and `robots.txt`
  present and valid; homepage exposes valid `Person`/`ProfilePage` JSON-LD (checked
  with Google's Rich Results Test); domain verified in Search Console.
